// we gotta create an immutable data structure

class Point {
    constructor(x , y) {
        this.x = x;
        this.y = y;
    }
}

const allPoints = new Map();

export function point(x , y) {
    const key = ``
    if(allPoints.has())
}